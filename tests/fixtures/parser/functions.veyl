pub fn birthday(mut user: User) -> User {
    user.age += 1;
    user
}

fn log_user(user: User) {
    print(user.name);
}

private fn parse_line(line: Str) -> Result<Pair, ParseError> {
    Pair
}

fn noop() -> () {}
