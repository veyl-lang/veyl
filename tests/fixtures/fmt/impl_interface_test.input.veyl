interface Reader{fn read(mut self,buf:Bytes)->Result<Int,IoError>;}
impl User{pub fn new(name:Str)->Self{Self{name}}fn rename(mut self,name:Str){self.name=name;}}
test "renames"{let user=User.new("Ada");assert(user.name=="Ada");}
