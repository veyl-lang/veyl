struct User {
    name: Str,
}

fn example() -> Str {
    let values = [1, 2];
    let first = values[0];
    let user = User { name: "Ada" };
    let nested = {
        let local = first;
        local
    };
    if nested == 1 {
        user.name
    } else {
        "none"
    }
}
